/*
  ═══╡ UPPGIFT 23 ╞══════════════════════════════════════════════════
  
  1. Kalla på funktionen `readline()` med textsträngen 
     `'Vad heter du: '` som argument, spara användarens input i en
     variabel.
  2. Använd variabeln som innehåller användarens input och
     skriv ut följande meddelande i konsolen:

     > Hej [namn]!

     Där [namn] är den input som användaren angav.
     Exempel: Om användaren anger namnet `Olav`, skall följande
     output skrivas ut:

     > Hej Olav!

  ───┤ RESURSER ├────────────────────────────────────────────────────

  - ...
*/

// Följande `import`-kodrad måste finns för att vi skall kunna fråga
// efter input av användaren. Obs! Ta inte bort denna kodrad.
import readline from 'read-console-input'

// Skriv din kod här..
