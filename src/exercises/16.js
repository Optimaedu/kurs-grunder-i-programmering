/*
  ═══╡ UPPGIFT 16 ╞══════════════════════════════════════════════════
  
  1. Definiera en konstant variabel med namnet `x` och tilldela 
     den talet `14`.
  2. Definiera en konstant variabel med namnet `y` och tilldela 
     den talet `6`.
  3. Definiera en konstant variabel med namnet `sum` och tilldela den
     ett uttryck som värde där du adderar variablerna `x` och `y`.
  4. Skriv ut variabeln `sum` i konsolen genom att använda 
     funktionen `console.log()`.

  ───┤ RESURSER ├────────────────────────────────────────────────────

  - ...
*/

// Skriv din kod här..